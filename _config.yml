title: Arthur Thuy
author:
  name: Arthur Thuy
  email: arthur.thuy@telenet.be
description: Personal website of Arthur Thuy

# Build settings

plugins:
  - jekyll-remote-theme
  - jekyll-seo-tag

# Use remote-theme plugin to load `minima` repository.
remote_theme: "jekyll/minima@43f3ed0"


# Theme-specific settings
minima:
  # Minima skin selection.
  # Available skins are:
  # classic	           Default, light color scheme.
  # dark	             Dark variant of the classic skin.
  # auto	             Adaptive skin based on the default classic and dark skins.
  # solarized-light	   Light variant of solarized color scheme.
  # solarized-dark	   Dark variant of solarized color scheme.
  # solarized	         Adaptive skin for solarized color scheme skins.
  skin: auto

  # If you want to link only specific pages in the site-navigation, use this and list
  # the `path` property (as represented via Liquid) of the pages in the order they should
  # be rendered.
  nav_pages:
    - pages/about.md
    - pages/education.md
    - pages/publications.md
    - pages/honors_awards.md
    - pages/cv.md

  # Social Media Links
  # Renders icons via Font Awesome Free webfonts CDN, based on ordered list of entries.
  #
  # Entry keys:
  # * title    Tooltip rendered on hovering over icon.
  # * icon     Font Awesome icon id. `github` corresponds to `fa-github`.
  # * url      Full URL of social profile.
  social_links:
    - title: GitHub page
      icon: github
      url: "https://github.com/arthur-thuy"
    - title: LinkedIn page
      icon: linkedin
      url: "https://www.linkedin.com/in/arthur-thuy/"
#   # Minima date format.
#   # Refer to https://shopify.github.io/liquid/filters/date/ if you want to customize this.
#   #
#   # date_format: "%b %-d, %Y"

# Set to `true` to show excerpts on the homepage.
# show_excerpts: true
